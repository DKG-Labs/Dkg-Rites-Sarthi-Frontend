/* eslint-disable */
import React from 'react';

const DKG_SectionHeader = ({ title }) => {
  return (
    <div className="dkg-section-header">
      <span className="dkg-section-title">{title}</span>
    </div>
  );
};

export default DKG_SectionHeader;
